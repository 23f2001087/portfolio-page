name: Cache
on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/cache@v4
        id: cache-deps
        with:
          path: ~/.cache/pip
          key: cache-525438c
      
      - name: prime-cache-525438c
        run: echo "Cache hit = ${{ steps.cache-deps.outputs.cache-hit }}"
